package dev.spiritstudios.aerobig.registry;


import dev.simulated_team.simulated.registrate.SimulatedRegistrate;
import net.minecraft.resources.ResourceLocation;

public class AerospaceRegistrate extends SimulatedRegistrate {
    public AerospaceRegistrate(ResourceLocation initialSection, String modId) {
        super(initialSection, modId);
    }
}